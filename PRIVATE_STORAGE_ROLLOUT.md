# 비공개 일러스트 전환 준비

## 운영 적용 결과 — 2026-09-08

사용자 승인 후 getIllustration 함수, 범위를 제한한 Hosting, Storage 규칙을 운영에 배포했습니다. 아래 적용 순서는 작업 기록용입니다.

- 일러스트 14,714개의 비공개 사본 생성 및 원본/사본 SHA-256 대조 완료.
- 기존·신규 이미지와 두 인덱스 총 29,430개 객체의 다운로드 토큰·공개 ACL 제거/부재 검증 완료. 기존 토큰 표본도 차단 확인.
- 기존 파일은 삭제하지 않았습니다. public이라는 경로 이름은 남지만 해당 일러스트와 소스 인덱스는 직접 읽기 차단 대상입니다.
- 운영 Hosting 버전: e00d8c538e9a7747. 이전 버전: cfd8a837a20ea1ae.
- Hosting은 기존 운영 20개 파일을 바탕으로 이미지 전달 변경 및 라이선스 고지만 반영한 39개 파일입니다. 로컬의 미배포 사진 검색 UI/모델 변경은 이번 Hosting에 포함하지 않았습니다.
- 운영 브라우저에서 단일 카드의 17종 일러스트 갤러리, 등록 화면 일러스트 선택 썸네일, 신규 API의 이미지 렌더링 확인. 카드 등록/수정은 실행하지 않았습니다.
- 이미지 관련 50개 및 Functions 52개 테스트 통과.

## 유지해야 할 상태

- GitHub 이미지 동기화 워크플로(ID 352017189)는 disabled_manually 상태입니다. 원격의 구 코드가 공개 경로에 다시 쓰는 것을 막기 위해 중지했습니다.
- 로컬 업로드 기본 경로와 캐시 정책은 비공개로 변경했습니다. 커밋·푸시는 하지 않았으므로 원격 동기화 코드는 아직 구 버전입니다. 검토 후 새 코드 반영을 승인받고 워크플로를 재개해야 합니다.
- 이미 외부에 저장된 파일/1년 캐시는 회수할 수 없습니다. 새 API는 브라우저 이미지 표시용 공개 API이며 개별 이미지 다운로드 자체를 막지는 않습니다.
- 이전 Hosting으로 단순 롤백하면 차단된 공개 Storage 경로를 다시 사용하므로 이미지 대체 경로가 깨질 수 있습니다. 현재 단계에서는 API 경로를 유지하는 수정 배포가 우선입니다.

## 준비된 코드

- private/illustrations/{cid}_{ciid}.webp와 private/illustrations-index.json을 서버 Admin SDK로 읽습니다.
- GET /api/illustrations/{cid}_{ciid}.webp는 이미지, /api/illustrations/{cid}.json은 한 카드의 표시 정보만 전달합니다. 전체 목록 API는 없습니다.
- 프런트엔드는 외부 CDN 우선, 신규 API 대체 경로를 사용합니다. 브라우저에 Storage 직접 URL·토큰은 보내지 않습니다.
- 응답은 private, no-store입니다. IP당 분당 180회 제한은 인스턴스별 보조 제한이며 분산 수집 방지 보장은 아닙니다. 로그인 전 검색을 유지하기 위해 공개 읽기 API로 구성했습니다.

## 적용 순서 (전환 당시 계획)

1. node scripts/prepare_private_illustrations.js로 운영 인덱스를 읽는 dry-run. --apply는 별도 승인 후 실행합니다. 사본 생성 전 원본 해시, 생성 후 사본 해시·캐시·토큰을 검사하며 공개본은 수정하지 않습니다. 기존 사본이 다르면 중단합니다.
2. 대상 Storage의 실제 규칙/IAM/ACL을 확인하고 비공개 경로가 익명 및 토큰으로 열리지 않는지 확인합니다. 현재 로컬 규칙은 public 밖을 차단하지만 이것만으로 운영 권한을 확정하지 않습니다.
3. getIllustration 함수와 Hosting을 함께 배포합니다. 운영 카드 검색·일러스트 갤러리·등록 화면 및 CDN 실패 대체 경로를 검증합니다. 현재 준비된 프런트엔드만 먼저 배포하면 사본/API 미준비로 이미지가 깨집니다.
4. 업로드·클라우드 동기화 작업의 목적지와 캐시 정책을 비공개 경로로 바꾸고 검증합니다. 현재 기존 동기화 스크립트는 공개 경로를 쓰므로 전환 완료 전 실행하면 안 됩니다.
5. 검증 후 기존 public/resources/illustrations 및 public/indexes/illustration_sources.json에 대한 읽기/목록 조회를 차단하고 다운로드 토큰을 폐기합니다. 공지 등 다른 public 자산의 접근은 유지해야 합니다. 광범위 public 허용 규칙과 좁은 거부 규칙을 겹쳐 쓰면 허용이 우선될 수 있으므로 허용 조건 자체에서 제외해야 합니다.
6. 기존 공개본은 비공개 사본 및 운영 검증 후 승인받아 정리합니다. 이미 브라우저 등에 남은 1년 캐시는 원격 삭제로 회수되지 않습니다.
7. 운영 익명 접근/목록/기존 토큰 차단, 정상 API 이미지 표시를 재검증합니다. 롤백은 공개본을 유지한 전환 검증 기간에 이전 Hosting/함수로 복귀하는 방식입니다.

## 최종 확인

운영 익명 요청 결과: 기존·신규 Storage 이미지와 소스 인덱스, 목록 조회 모두 403. 신규 이미지 API와 카드별 메타데이터 API는 200, 전체 인덱스 API는 404입니다.

운영 버킷 IAM과 지정된 모든 객체의 ACL/토큰 검증, 화면 전환은 완료했습니다. 예약 동기화의 원격 코드 반영 및 재개는 미완료이며 공개본 삭제는 수행하지 않았습니다.
