# 사진 검색 검증 — 2026-09-08

## 현재 반영한 변경 (로컬, 미배포)

- ONNX Runtime의 WASM 모듈 경로를 절대 URL로 수정. 기존 bare specifier 오류 때문에 학습형 검출 대신 OpenCV가 실행되던 문제를 브라우저에서 재현하고 수정했습니다.
- 네 꼭짓점을 실제 매칭용 일러스트 crop에 반영하는 원근 보정 추가. 메인 스레드의 OpenCV 초기화를 요구하지 않는 제한된 크기의 픽셀 샘플링입니다.
- EXIF 회전 후 크기를 사용하는 평가 스크립트 수정. 기본 일러스트 crop이 그림 하단을 잘라내던 범위를 보완했습니다.
- 검출기 초기화는 사진 선택 시 시작합니다. 일반 로그인/검색 시 불필요한 WASM 초기화를 하지 않습니다.
- 메인 검색은 업로드 후 자동 분석하고 이름 후보 선택 목록으로 전환합니다. 전용 분석 로딩, 이전 작업 결과 무시, 워커 실패 후 재시도, 이미지 API 시간 제한을 보완했습니다.
- 사진 표시 영역과 검출 좌표가 어긋나던 이미지 높이 제한을 제거했습니다. 바깥 클릭 시 사진 패널이 닫히는 것을 localhost:5005에서 확인했습니다.

## 검증 범위

- 전체 자동 테스트 123개 통과.
- 사용자가 제공한 단독 사진은 모델 평가에서 1개, 복수 사진은 5개 검출.
- localhost:5005에서 학습형 모델 초기화/실행, 로그인 복원, 5개 영역의 검색 후보 화면과 바깥 클릭 닫기를 확인했습니다.
- 운영 Storage pHash 인덱스로 평가했습니다. 로컬 DB에 없는 카드의 이름 대신 CID가 나오는 현상은 사용자의 지시에 따라 수정 대상에서 제외했습니다.
- pHash는 복수 사진의 슈팅 퀘이사 드래곤(9709_2)과 반사가 적은 하얀 용의 낙윤(22127_1)을 최상위로 반환했습니다. 다른 반사 강한 카드의 순위는 불안정하며 단독 사진도 정답이 상위 5개에 없었습니다. 인식 기능 전체가 완성된 상태가 아닙니다.

## 부분 특징 비교 실험 (서비스 미연결)

기존 OpenCV의 ORB 특징과 RANSAC 원근 일관성 검사를 사용했습니다. 정답 참조와 일부 pHash 오답 참조를 함께 비교한 제한된 실험이며 전체 데이터셋 검색 정확도로 해석해서는 안 됩니다.

| 사진 영역 | 정답 참조 | 정답의 일관된 특징점 | 비교한 오답의 최대 |
| --- | --- | ---: | ---: |
| 복수 사진 상단 좌측 | 9709_2 | 239 | 4 |
| 복수 사진 하단 좌측 | 22144_1 | 53 | 4 |
| 복수 사진 하단 중앙 | 22127_1 | 126 | 4 |
| 복수 사진 상단 우측 | 22559_1 | 106 | 4 |
| 복수 사진 하단 우측 | 22127_1 | 390 | 0 |
| 단독 사진 | 22694_1 | 39 | 4 |

실험 도구: scripts/evaluate_photo_features.js. 평가용 참조는 로컬 일러스트 파일을 읽지만 서비스의 데이터 경로를 로컬로 변경한 것은 아닙니다.

## 남은 작업

1. 부분 특징을 전체 일러스트 데이터에서 검색할 인덱스 설계·생성 및 검색 비용 검증. pHash 상위 5개만 재정렬하면 정답 누락을 해결할 수 없습니다.
2. 검증된 검색 방식을 공통 분석 API와 연결하고 반사/회전/단독/복수 표본으로 회귀 검사.
3. 모바일 및 사진 등록 흐름의 최종 UI 검증. 이번 변경만으로 전체 등록 흐름을 검증했다고 보지 않습니다.
4. 최종 변경 범위 검토 후 커밋·푸시 승인. 이번 작업에서 커밋·푸시 및 사진 검색 배포는 하지 않았습니다.
