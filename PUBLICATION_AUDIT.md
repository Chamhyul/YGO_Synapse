# 공개 범위 점검

## 서비스 계정 키 교체 및 기존 키 비활성화 (2026-09-08)

- Firebase 사용자 재로그인 후 키 관리 권한을 확인했습니다. 동일 서비스 계정에 새 키를 발급하고 비공개 Storage 읽기를 검증한 뒤 로컬 functions/serviceAccountKey.json을 원자적으로 교체했습니다. 파일 권한은 0600입니다.
- IAM 목록에서 기존 키 disabled=true, 새 키 disabled=false를 확인했습니다. 기존 키로 새 액세스 토큰을 발급받는 요청은 거부됐습니다. 운영 /api/illustrations/4007_1.webp는 HTTP 200 image/webp였습니다.
- 기존 키는 아직 영구 삭제하지 않았으며 접근 제한된 임시 디렉터리에 복구 사본을 보관했습니다. 이미 발급된 토큰의 즉시 무효화나 기존 프로세스 재시작을 수행한 것은 아닙니다. 로컬 장기 실행 프로세스는 재시작 후 새 키를 사용해야 합니다.
- GitHub Secret 갱신일(2026-08-14)은 기존 로컬 키 생성일(2026-08-25)보다 앞섭니다. 같은 키로 보이지 않지만 Secret 원문 대조는 불가능하며 이번에 GitHub Secret은 수정하지 않았습니다.
- 과거 빌드 소스/아티팩트의 키 파일 제거, 기존 키 영구 삭제, 앞서 출력된 Firebase 로그인 인증의 서버 측 철회 확인은 후속 작업입니다. 재로그인 성공만으로 이전 인증의 철회를 확인했다고 보지 않습니다.

## 서비스 계정 키 정리 착수 (2026-09-08)

- 배포 소스에서 키 파일이 확인된 함수는 searchCardByImage와 getIllustration입니다. 보관된 원본 ZIP의 키는 현재 로컬 functions/serviceAccountKey.json과 동일함을 값 출력 없이 대조했습니다.
- 운영 함수 45개의 실행 서비스 계정은 해당 키의 서비스 계정과 모두 다릅니다. 로컬 에뮬레이터와 업로드/동기화/회원 CSV 스크립트는 로컬 키 파일을 사용하는 경로가 남아 있습니다.
- GitHub의 FIREBASE_SERVICE_ACCOUNT_YGO_SYNAPSE Secret이 존재하며 2026-08-14에 갱신됐습니다. GitHub는 기존 Secret 값을 반환하지 않으므로 로컬 키와 동일한지 확인되지 않았습니다.
- 로컬 키 및 키가 포함된 임시 원본 ZIP의 파일 권한을 0600으로 제한했습니다. 키 폐기나 파일 삭제는 아직 수행하지 않았습니다.
- 서비스 계정 인증의 IAM 키 목록 조회는 HTTP 403, 기존 사용자 액세스 토큰 조회는 HTTP 401입니다. 관리 가능한 사용자 인증을 확보하고 로컬/CI 인증을 전환한 뒤 기존 키를 비활성화·검증·폐기해야 합니다.
- 점검 중 Firebase CLI login:list --json 명령이 인증 토큰을 도구 출력에 포함했습니다. 이 문서에는 값을 기록하지 않았습니다. 해당 로컬 Firebase 로그인 인증도 폐기 및 재로그인 대상으로 추가합니다.

## 운영 소스 공개 전 추가 발견 (2026-09-08)

Cloud Functions API로 함수 45개의 실제 배포 ZIP을 조사한 결과, 2개 함수에 대응하는 배포 소스에 실제 개인키를 포함한 serviceAccountKey.json이 있었습니다. 이 사실은 Hosting 공개 파일 검사와 다른 범위의 발견입니다. 비공개 빌드 소스에 존재했다는 사실만으로 외부 유출이 확인된 것은 아닙니다.

원본 ZIP은 공개하지 않았습니다. 공개용 소스에서는 해당 키 파일을 제외하고 전체 코드의 개인키/토큰 패턴, 민감 리터럴 및 로컬 비밀값 대조 검사를 통과시켰습니다. 원본·공개본 해시와 제외 경로는 소스 묶음의 FUNCTIONS.json에 기록했습니다. Functions 배포 ignore에도 인증 파일과 키 파일 패턴을 추가했습니다.

기존 빌드 소스의 키와 실제 IAM 키는 변경/폐기하지 않았습니다. 키 교체는 사용하는 배치·로컬 환경을 확인한 뒤 별도 승인하에 진행해야 합니다. 로컬 ignore 수정은 이미 보관된 빌드 소스를 소급해서 정리하지 않습니다.

## 비공개 전환 후 상태 (2026-09-08)

이 절의 결과가 아래 전환 전 기록보다 최신입니다. 일러스트 14,714개를 private/illustrations로 복사하고 전체 원본·사본 해시를 대조했습니다. 기존·신규 객체 및 소스 인덱스 총 29,430개의 다운로드 토큰·공개 ACL 제거/부재를 검증했습니다. 파일 삭제는 없습니다.

운영 규칙은 기존 일러스트와 소스 인덱스의 get 및 public 목록 조회를 차단합니다. 일반 공개 카드 데이터는 유지합니다. Hosting은 e00d8c538e9a7747로 전환됐으며 배포 파일 39개 목록에서 비밀파일 이름·일러스트 원본 경로가 발견되지 않았습니다. 파일 목록 검사는 모든 내용/백엔드 환경변수의 보안감사와는 구분합니다.

운영 갤러리·등록 화면 썸네일·신규 API 이미지 렌더링을 확인했습니다. 원격 이미지 동기화는 구 코드 재실행 방지를 위해 일시 중지 상태입니다. 상세 상태: PRIVATE_STORAGE_ROLLOUT.md.

## 운영 환경 추가 확인 (2026-09-08)

- 배포된 Storage 규칙을 Firebase Rules API로 직접 조회했습니다. public/**는 익명 읽기 허용, 나머지 경로는 클라이언트 읽기/쓰기 차단입니다.
- 운영 버킷 IAM에 allUsers/allAuthenticatedUsers 바인딩은 없었습니다. Uniform bucket-level access는 비활성화되어 있어 객체 ACL은 별도 확인 대상입니다. Public access prevention은 inherited이며 상위 정책의 실효 상태까지 확인한 것은 아닙니다.
- 익명 HTTP 검사에서 일러스트 표본과 소스 인덱스는 206, 파일 목록은 200, 검색 pHash 인덱스 및 일반 GCS 표본 URL은 403이었습니다.
- 표본 객체에 다운로드 토큰이 있고 Cache-Control은 public, max-age=31536000, immutable입니다. 토큰 값은 기록하지 않았습니다.
- 운영 인덱스를 읽는 비공개 전환 dry-run에서 14,714개 항목의 경로·해시 형식을 확인했습니다. 이미지 전체 내용 해시 검증이나 사본 생성은 아직 하지 않았습니다.
- Hosting의 /functions/.secret.local과 /resources/illustrations/10000_1.webp는 파일이 아닌 SPA HTML을 반환했습니다. /.env, /.git/config, /serviceAccountKey.json 요청은 통신 실패여서 공개 여부를 확정하지 못했습니다.
- 아래는 최초 로컬 점검 기록입니다. 그중 운영 IAM/배포 규칙 미조회라는 범위 제한은 위 확인으로 보완됐습니다. 객체별 ACL 전체, 원격 전용 Git 이력과 배포 artifact 검사는 남아 있습니다.

점검일: 2026-09-08. 로컬 작업 트리 및 로컬에 존재하는 전체 Git ref 기준입니다. 민감한 실제 값은 이 보고서와 도구 출력에 기록하지 않았습니다.

## 결과

| 범위 | 확인 결과 |
| --- | --- |
| Git 추적 파일 | 점검 시작 시 108개. 비밀키·토큰 및 카드 일러스트 파일 발견 없음 |
| 공개 가능 미추적 파일 | 최초 검사 시 33개. 고지 문서·스크립트·모델/런타임 중심. 비밀키 발견 없음 |
| 로컬 Git 이력 | 전체 ref에서 478개 객체 중 298개 blob 검사. 비밀키·토큰 및 일러스트 경로 발견 없음 |
| Hosting의 public 폴더 | 검사 당시 48개 파일. 카드 JPEG/WebP 및 로컬 비밀값 발견 없음. 이후 추가한 파일은 라이선스 원문과 고지 문서 |
| 로컬 인증 파일 | functions/.env.local 및 functions/.secret.local은 Git에서 제외되며 functions 배포 ignore의 *.local에도 해당. SECRET/TOKEN 등 민감 필드 값 4개를 추적 파일·public·이력에 대조한 결과 일치 없음 |
| 로컬 일러스트 | resources/illustrations 내 WebP 14,714개. Git 제외 대상이며 Hosting의 public 폴더 밖에 위치 |
| 로컬 메타데이터/검색 인덱스 | data/illustration-sync는 Git 제외 대상이며 Hosting 공개 폴더 밖에 위치 |
| Storage 일러스트 | storage.rules는 public/**에 익명 읽기 및 목록 조회를 허용. 업로드 스크립트의 목적지는 public/resources/illustrations. Git 공개 여부와 별개로 웹 서비스에서 읽도록 설계된 자산 |
| Storage 검색 인덱스 | 서버 코드의 경로는 system/illustration-search/phash.json. 로컬 Storage 규칙상 클라이언트 읽기/쓰기 차단, 서버 Admin SDK 사용 |
| Firestore | 로컬 firestore.rules는 클라이언트 직접 읽기/쓰기를 모두 차단. 서버 Admin SDK의 권한과 API별 접근 제어는 별도 |

## 검사 방법과 구분

- 개인키 헤더, Google OAuth client secret, GitHub/AWS 토큰 형식, 민감 필드의 문자열 리터럴 패턴을 검색했습니다. 로컬 비밀값을 직접 대조하는 검사도 병행했습니다.
- OpenCV.js에서 AWS 키 형식과 우연히 일치한 문자열 1건은 내장 WASM의 base64 내부에만 존재했습니다. 실행 코드의 인증정보 리터럴이 아닌 패턴 오탐으로 분류했습니다.
- Firebase 프로젝트/버킷 이름, OAuth client ID, 광고 게시자 ID 등 공개 식별자는 비밀키와 구분했습니다. 공급자 접근 제한과 서버 권한 설정을 대신하는 비밀정보로 취급하지 않았습니다.
- public의 앱 아이콘 PNG/ICO와 ONNX 모델은 웹 제공 대상입니다. 카드 일러스트 원본 파일과 구분했습니다. 모델 파라미터의 학습 데이터 재현 가능성까지 검사한 것은 아닙니다.
- 새 고지 문서에는 사용자 DB·인증값이 없으며, 유지관리 메모는 public에서 제거했습니다. 루트 메모는 Git에 포함되면 공개되는 문서이므로 비밀을 기록하지 않습니다.

## 주의할 공개 경로

1. **Storage 일러스트는 비공개가 아닙니다.** 이번 요청에서는 규칙을 변경하거나 파일을 삭제하지 않았습니다. 직접 다운로드까지 막으려면 이미지 표시 방식과 인증 정책을 함께 변경해야 합니다. 브라우저에 표시되는 이미지를 완전히 복사 불가능하게 만들 수는 없습니다.
2. .gitignore는 Git에만 적용됩니다. 폴더 전체를 압축해 공유하면 제외된 인증 파일·일러스트가 들어갈 수 있습니다. 공유 아카이브의 실제 파일 목록을 별도로 확인해야 합니다.
3. 현재 ignore는 .env.local, *.local 등을 제외하지만 모든 이름의 인증 파일을 포괄하지 않습니다. 예를 들어 .env.production이나 임의 이름의 서비스 계정 JSON은 자동 보호된다고 가정하지 마십시오. 현재 공개 범위에서 해당 파일은 발견되지 않았습니다.
4. 로컬 전체 ref는 검사했지만 원격에만 있는 ref, 삭제되어 접근되지 않는 원격 이력, GitHub release/Actions artifact, 배포된 서버의 환경변수·IAM·다운로드 토큰은 점검 범위 밖입니다. 원격 Storage의 실제 배포 규칙과 개별 객체 ACL은 이번에 조회하지 않았습니다.

발견 없음은 모든 종류의 비밀정보가 없다는 보증은 아닙니다. 이번 작업에서는 커밋·푸시·배포, 키 폐기, Storage 권한 변경 및 데이터 삭제를 수행하지 않았습니다.
